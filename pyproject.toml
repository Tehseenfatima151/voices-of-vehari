[project]
name = "voices_of_vehari"
version = "1.0.0"
dependencies = [
    "Flask>=3.0.0",
    "Flask-SQLAlchemy>=3.1.0",
    "Flask-JWT-Extended>=4.6.0",
    "Flask-CORS>=4.0.0",
    "psycopg2-binary>=2.9.9",
    "Werkzeug>=3.0.0",
    "python-dotenv>=1.0.0"
]

[tool.vercel]
entrypoint = "api.index:app"
